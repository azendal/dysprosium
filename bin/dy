#!/usr/bin/env ruby
$LOAD_PATH.unshift(File.dirname(__FILE__))
$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))
require 'dysprosium'

if ARGV.empty?
  puts "Print help"
else
  Dysprosium.parse(ARGV)
end
